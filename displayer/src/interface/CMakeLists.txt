set(RUST_INPUT test.rs)
set(RUST_OUTPUT libtest.so)

EXECUTE_PROCESS(COMMAND rustc --crate-type=cdylib ${CMAKE_CURRENT_SOURCE_DIR}/${RUST_INPUT} -o ${CMAKE_CURRENT_BINARY_DIR}/${RUST_OUTPUT})

TARGET_LINK_LIBRARIES(displayer INTERFACE ${CMAKE_CURRENT_BINARY_DIR}/${RUST_OUTPUT})